-- Template for future Supabase migrations.
-- Copy this pattern into the next numbered file when making database changes.
-- Example filename: 15_add_review_notes.sql

-- Purpose:
-- Describe what this migration changes and why it is needed.

-- Safety guidance:
-- 1. Prefer additive/idempotent changes where possible.
-- 2. Use IF NOT EXISTS / DROP ... IF EXISTS when supported.
-- 3. Keep schema and seed changes committed in this folder.

-- Example structure:
-- ALTER TABLE public.example_table
-- ADD COLUMN IF NOT EXISTS example_column TEXT;

-- UPDATE public.example_table
-- SET example_column = 'default value'
-- WHERE example_column IS NULL;
